# خطوات تفعيل قاعدة البيانات الخاصة بالمشروع (بدل Firebase)
+ حماية الرفع والتعديل بكلمة سر

## مهم: المشروع الحالي اللي شغالة عليه هو
`https://refunds-six.vercel.app/`
كل الخطوات اللي تحت لازم تتعمل على المشروع ده بالذات في لوحة تحكم Vercel
(مش أي مشروع قديم بنفس الكود).

الفكرة: البيانات دلوقتي بتتخزن في قاعدة بيانات Postgres **مربوطة مباشرة بمشروعك على Vercel**،
مش في حساب خارجي منفصل. أي حد يفتح لينك الداشبورد هيشوف نفس الأرقام لحظيًا.

## 1) استبدال ملفات المشروع
انسخ كل الملفات دي فوق نسخة الريبو بتاعتك على GitHub (أو اعمل commit ليها):
- `api/` (مجلد جديد بالكامل — فيه دوال الحفظ والجلب)
- `src/utils/cloudSync.ts` (اتعدل)
- `src/db/index.ts` (اتعدل)
- `src/db/drizzle.config.ts` (اتعدل)
- `package.json` (اتعدل — شيل firebase وضاف @vercel/node)
- `.env.example` (اتعدل)
- (اتشال) `src/lib/firebase.ts`

## 2) أنشئ قاعدة بيانات Postgres من نفس Vercel
1. افتح مشروعك على vercel.com → تبويب **Storage**.
2. اضغط **Create Database** → اختار **Postgres** (Powered by Neon).
3. اختار اسم للقاعدة واضغط **Create**، بعدين **Connect** لمشروعك.
4. Vercel هيضيف متغير `POSTGRES_URL` (وغيره) تلقائيًا لإعدادات المشروع — مش محتاج تعمل حاجة يدوي.

## 3) اعمل Redeploy
بعد ما ترفع الكود الجديد على GitHub، Vercel هيعمل Deploy تلقائي.
لو مش حصل، من تبويب **Deployments** اضغط **Redeploy** على آخر نسخة.

## 4) جرب
- افتح اللينك، ارفع شيت اكسيل.
- افتح نفس اللينك من جهاز/متصفح تاني (أو موبايل) — المفروض تشوف نفس البيانات فورًا.

## ملاحظات
- الجداول بتتعمل تلقائيًا أول مرة تستخدم فيها أي API (مفيش خطوة migration يدوية لازمة).
- لو حبيت تشغل المشروع محليًا على جهازك بره Vercel، خد الـ connection string من تبويب Storage
  وحطه في ملف `.env` باسم `POSTGRES_URL`.
- ملفات Firebase (`firebase-applet-config.json`, `firestore.rules`, `firebase-blueprint.json`)
  بقت مش مستخدمة، تقدر تسيبها أو تمسحها، مش هتأثر على حاجة.

## 5) تفعيل حماية الرفع/التعديل بكلمة سر
دلوقتي أي حد يفتح اللينك يقدر يشوف الداشبورد بس، ولو دوس على أيقونة الشاشة (وضع العرض)
عشان يظهر أدوات الرفع/التعديل، هيتطلب منه كلمة سر — وحتى لو حاول يبعت طلب مباشر للـ API
من غير الواجهة، السيرفر نفسه هيرفضه من غير الباسورد الصح.

1. من لوحة تحكم مشروعك على Vercel → **Settings** → **Environment Variables**.
2. ضيفي متغير جديد اسمه `ADMIN_PASSWORD` وقيمته كلمة السر اللي تختاريها.
3. تأكدي إن Environment مفعّلة على **Production** (وممكن كمان Preview لو بتستخدميه).
4. اعملي **Redeploy** لآخر نسخة عشان المتغير يشتغل.
5. افتحي اللينك، دوسي على أيقونة الشاشة، هيظهرلك مربع كلمة السر — اكتبيها وادخلي.
6. كلمة السر بتفضل محفوظة في المتصفح لحد ما تقفلي التاب (مش هتتسأل تاني في نفس الجلسة).

**ملاحظة أمان:** ده نظام بسيط بكلمة سر واحدة مشتركة (مش حسابات مستخدمين منفصلة) —
كافي عشان يمنع أي حد عادي من التعديل، لكن مش بديل عن نظام تسجيل دخول حقيقي لو
احتجتيه مستقبلًا (مثلاً لو عايزة كل شخص يكون له صلاحياته الخاصة).
